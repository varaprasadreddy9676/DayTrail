[package]
name = "worktrace-sessionizer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
worktrace-core = { path = "../core" }

[lib]
name = "worktrace_sessionizer"
path = "src/lib.rs"

